# CITYNEXUS AI — Competition Prototype

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

The application contains a polished command-center dashboard and simulated AI data for flood, mobility, waste, citizen services, governance copilot and SDG impact.
